// export const config = Object.freeze({
//   backendBaseUrl: `http://localhost:9000/`,
//   webSocketUrl: `ws://localhost:9000/graphql`,
// })

// export const config = Object.freeze({
//   backendBaseUrl: `http://${window.location.host}/`,
//   webSocketUrl: `ws://${window.location.host}/ws/`,
// })

export const config = Object.freeze({
  backendBaseUrl: `https://${window.location.host}/`,
  webSocketUrl: `wss://${window.location.host}/ws/`,
})

