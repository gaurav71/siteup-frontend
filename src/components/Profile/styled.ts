import styled from 'styled-components'

export const Wrapper = styled.div`
  padding: 30px;
  position: absolute;
  left: 50%;
  transform: translateX(-30%);

  .ant-form-item {
    width: 550px;
  }

  .ant-form-item-label {
    max-width: 100%;
  }
`
